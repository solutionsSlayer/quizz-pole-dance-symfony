{% extends 'base.html.twig' %}

{% block title %}Edit Question{% endblock %}

{% block body %}
    <div class="m-5">
        <h1>Edit Question</h1>
        
        <div>
            {{ include('question/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="d-flex">
            <a class="mr-2 btn btn-light text-decoration-none" href="{{ path('list_question') }}">back to list</a>
            {{ include('question/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
